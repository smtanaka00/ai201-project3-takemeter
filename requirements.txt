# TakeMeter — local dependencies for the verification & error-analysis scripts.
# The fine-tuning/baseline notebook runs in Colab and installs its own heavier deps
# (transformers, torch, groq) there; these are only what scripts/ and src/ need locally.

pandas>=2.0
scikit-learn>=1.3
# scrape_reddit.py uses Reddit's public JSON endpoints via stdlib urllib — no API
# credentials required. certifi supplies a portable CA bundle so TLS verification works
# on machines (e.g. stock macOS Python) that can't find system certificates.
certifi>=2023.0
